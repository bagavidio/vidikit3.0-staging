import { ColorSystem } from "@vidikit/ui-react";

export const metadata = {
  title: "Color System · vidikit",
  description: "VIDI primitive color palette — 8 families, 7 shades each.",
};

export default function ColorsPage() {
  return <ColorSystem />;
}
